import { NumberAnimatorPipe } from './number-animator.pipe';

describe('NumberAnimatorPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberAnimatorPipe();
    expect(pipe).toBeTruthy();
  });
});
